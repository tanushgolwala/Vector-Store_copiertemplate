_min_copier_version: "7.0"
_subdirectory: "{{ project_slug }}"

project_name:
  type: str
  help: What is the name of your project?

project_slug:
  type: str
  default: "{{ project_name|lower|replace(' ', '_') }}"
  help: Slug for directory/project name (no spaces or special characters)
